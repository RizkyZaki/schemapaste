# Renderers Module

Renderer adapters consume normalized schema models only.

Planned responsibilities:

- Transform normalized schema into React Flow nodes/edges.
- Apply per-workspace line customization policies.
- Handle graph-level decoration (cardinality labels, path reroutes).
